# Команды для выгрузки проекта на сервер (Деплой)

1. Переход в папку проекта на сервере:
`cd /root/chalama_project`

2. Получение последних изменений из репозитория (ветка main):
`git pull origin main`

3. Перезапуск всех процессов (Node.js/PM2):
`pm2 restart all`
